# Akeneo AI Enrichment - Iframe Version
# Environment Configuration

# Akeneo PIM Configuration
# Get these from your Akeneo instance settings
AKENEO_BASE_URL=https://your-instance.akeneo.cloud
AKENEO_CLIENT_ID=your_client_id
AKENEO_SECRET=your_client_secret
AKENEO_USERNAME=your_username
AKENEO_PASSWORD=your_password

# Note: For production, use OAuth token-based authentication
# The current implementation supports both:
# 1. OAuth credentials (client_id + secret + username + password)
# 2. Pre-generated access token (pass directly to akeneoApi)

# Deployment Configuration
# NODE_ENV=production
